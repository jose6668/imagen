pipeline {
    agent any
    stages {
        stage('Construir imagen Docker') {
            steps {
                script {
                    docker.build('mi-imagen-jenkins')
                }
            }
        }
    }
}